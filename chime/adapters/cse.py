"""Adapter for cse.lk's undocumented JSON endpoints (prices, market summary).

Not implemented yet — see CLAUDE.md build order step 1: verify each endpoint
still works and log real sample responses to docs/sample_responses/ first.
"""
