"""Alert rule engine: evaluates each new snapshot against active alert rules.

v1 rule types: price crosses above/below X, daily % move exceeds X,
new disclosure published for a watched symbol.

Not implemented yet — see CLAUDE.md build order.
"""
