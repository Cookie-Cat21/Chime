"""Market-hours polling loop (09:30–14:30 SLT, weekdays).

Fetches price + disclosure data via the adapters, normalizes it, stores every
snapshot, and hands new snapshots to the rule engine.

Not implemented yet — see CLAUDE.md build order.
"""
